{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Consul ESM",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "args": ["--config-file=${workspaceFolder}/config.hcl"]
        }
    ]
}